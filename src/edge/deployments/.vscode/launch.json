{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "event_generator Remote Debug (Python)",
      "type": "python",
      "request": "attach",
      "port": 5678,
      "host": "localhost",
      "logToFile": true,
      "redirectOutput": true,
      "pathMappings": [
        {
          "localRoot": "${workspaceFolder}/modules/event_generator",
          "remoteRoot": "/app"
        }
      ],
      "windows": {
        "pathMappings": [
          {
            "localRoot": "${workspaceFolder}\\modules\\event_generator",
            "remoteRoot": "/app"
          }
        ]
      }
    }
  ]
}